// Get the button element using the DOM
var button = document.getElementById('myButton');

// Add an event listener to the button
button.addEventListener('click', function() {
    // Event listener function
    alert('Button clicked!');
});
